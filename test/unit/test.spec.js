describe("Shouldn't integration test be better for GraphQL service test??", () => {
  it("Or does it make sense to test schema in unit test?", () => {
    const value = true;
    value.should.eql(true);
  })
})
